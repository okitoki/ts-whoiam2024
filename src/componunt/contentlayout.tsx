import React from 'react';
import PropTypes from 'prop-types';
import { ContentLayout } from '../model/type';



function ContentLayout({id}:ContentLayout) {
    return (
        <div>
            
        </div>
    );
}

export default ContentLayout;